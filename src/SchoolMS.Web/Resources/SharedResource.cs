namespace SchoolMS.Web;

/// <summary>
/// Marker class for shared localization resources.
/// The namespace must match the project root namespace (SchoolMS.Web)
/// so the resource files in the Resources folder are found correctly
/// when ResourcesPath = "Resources" is configured.
/// </summary>
public class SharedResource
{
}
