@using SchoolMS.Application.DTOs
@{
    var isEdit = Model != null;
    var item = isEdit ? (StudentBehaviorDto)Model : null;
    var students = ViewBag.Students as List<StudentDto> ?? new();
    var isSuperAdmin = (bool)(ViewBag.IsSuperAdmin ?? false);
    var schools = ViewBag.Schools as List<SchoolDto> ?? new();
}

<div class="d-flex justify-content-between align-items-center mb-4" data-aos="fade-down">
    <h4 class="mb-0"><i class="fas @(isEdit ? "fa-edit" : "fa-plus-circle") me-2 text-primary"></i>@(isEdit ? Localizer["EditRecord"] : Localizer["AddRecord"])</h4>
    <a href="@Url.Action("Index")" class="btn btn-secondary"><i class="fas fa-arrow-left me-1"></i>@Localizer["Back"]</a>
</div>
<div class="dashboard-card card p-4" data-aos="fade-up">
    <form asp-action="@(isEdit ? "Edit" : "Create")" method="post">
        @Html.AntiForgeryToken()
        @if (isEdit) { <input type="hidden" name="Id" value="@item!.Id" /> }
        <div class="row g-3">
            @if (isSuperAdmin)
            {
                <div class="col-md-4">
                    <label class="form-label">@Localizer["School"] *</label>
                    <select name="SchoolId" class="form-select select2" required>
                        <option value="">-- @Localizer["Select"] --</option>
                        @foreach (var s in schools) { <option value="@s.Id" selected="@(item?.SchoolId == s.Id)">@s.Name</option> }
                    </select>
                </div>
            }
            <div class="col-md-4">
                <label class="form-label">@Localizer["Student"] *</label>
                <select name="StudentId" class="form-select select2" required>
                    <option value="">-- @Localizer["SelectStudent"] --</option>
                    @foreach (var s in students) { <option value="@s.Id" selected="@(item?.StudentId == s.Id)">@s.FullName</option> }
                </select>
            </div>
            <div class="col-md-4">
                <label class="form-label">@Localizer["Type"]</label>
                <select name="Type" class="form-select">
                    @foreach (var t in Enum.GetValues<BehaviorType>()) { <option value="@t" selected="@(item?.Type == t)">@t</option> }
                </select>
            </div>
            <div class="col-md-4"><label class="form-label">@Localizer["Points"]</label><input type="number" name="Points" class="form-control" value="@(item?.Points ?? 0)" /></div>
            <div class="col-md-6"><label class="form-label">@Localizer["Title"] *</label><input type="text" name="Title" class="form-control" value="@item?.Title" required /></div>
            <div class="col-md-3"><label class="form-label">@Localizer["Date"] *</label><input type="date" name="IncidentDate" class="form-control" value="@(item?.IncidentDate.ToString("yyyy-MM-dd") ?? DateTime.Today.ToString("yyyy-MM-dd"))" required /></div>
            <div class="col-md-3"><div class="form-check form-switch mt-4"><input class="form-check-input" type="checkbox" name="NotifyParent" value="true" @(item?.NotifyParent == true ? "checked" : "") /><label class="form-check-label">@Localizer["NotifyParent"]</label></div></div>
            <div class="col-md-6"><label class="form-label">@Localizer["Description"]</label><textarea name="Description" class="form-control" rows="2">@item?.Description</textarea></div>
            <div class="col-md-6"><label class="form-label">@Localizer["ActionTaken"]</label><textarea name="ActionTaken" class="form-control" rows="2">@item?.ActionTaken</textarea></div>
        </div>
        <div class="mt-4 d-flex gap-2">
            <button type="submit" class="btn btn-accent"><i class="fas fa-save me-1"></i>@Localizer["Save"]</button>
            <a href="@Url.Action("Index")" class="btn btn-secondary">@Localizer["Cancel"]</a>
        </div>
    </form>
</div>
